# Per-pack launcher paths (see .env.example)
**/.env
**/.env.example

**/.probe

# Exclude exported Modrinth modpacks
*.mrpack

# Exclude .direnv
.direnv

# Eclude precommit hooks config
.pre-commit-config.yaml
*_old

test
test/*

**/dump/

*/*.zip
*/extract/*
extract/*

.cursor